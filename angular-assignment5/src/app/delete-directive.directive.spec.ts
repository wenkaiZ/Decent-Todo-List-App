import { DeleteDirectiveDirective } from './delete-directive.directive';

describe('DeleteDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
