import { FinishDirectiveDirective } from './finish-directive.directive';

describe('FinishDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FinishDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
